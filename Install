 #!/bin/sh
if [ "${MGICONFIG}" = "" ]
then
    MGICONFIG=/usr/local/mgi/live/mgiconfig
    export MGICONFIG
fi

. ${MGICONFIG}/master.config.sh

# Play around with JAVA_HOME
JAVA_HOME=/usr/mgijava7
export JAVA_HOME

ant
